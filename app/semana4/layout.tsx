import type { ReactNode } from "react"
import ClientLayout from "../components/client-layout"

export default function Semana4Layout({ children }: { children: ReactNode }) {
  return <ClientLayout>{children}</ClientLayout>
}
